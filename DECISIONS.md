# Decisions

> Append-only ledger of settled and reversed decisions. Format:
> `YYYY-MM-DD — what · reversed-from (if any) · reason/principle`.
> Never rewrite, reflow, or reorder; correct by appending a dated line that
> points back. Moved here from docs/SUMMARY.md on 2026-09-22 (pass 20260922);
> lines before that date keep the stage numbering in force when written.
> Promote to docs/adr/ only if an entry ever needs paragraphs.

- 2026-07-16 — `MCP_TRANSPORT` value-authoritative · reversed from locked presence-only · make illegal states unrepresentable
- 2026-07-16 — `MCP_PORT` validation branch-scoped to streamable-http (not global) · symmetry with unvalidated `MCP_HOST`; "stdio ignores host/port" = not inspected
- 2026-07-16 — config resolver in new `config.py` · reversed from locked "logic in `server.py`" · separation of concerns + pure-resolver testability
- 2026-07-16 — transport string `streamable-http` over `http` alias · explicit, consistent naming
- 2026-07-17 — Doc Update Process v2: two-tier scratchpad + batch · Step 0 generate-at-end kept silently failing · honor capture-as-you-happen via append-as-you-go
- 2026-07-17 — ownership matrix canonical in CLAUDE.md · was scattered (SUMMARY/memory/CLAUDE) · single in-repo source of truth
- 2026-07-17 — doc-pass apply mechanics split on the git boundary (three branches) · in-repo → Claude Code applies + apostrophe-greps + commits (atomic where git lives); out-of-repo memory → written directly, no commit; auto-memory → memory system · supersedes the earlier two-branch rule; ownership matrix gains Author/Executor columns
- 2026-07-17 — global curated memory → joint ownership (project chat + Claude Code) · was project-chat-sole (an artifact of the Filesystem-access framing) · both chats generate user-insight; unversioned-clobber risk handled by read-before-write + additive + batch reconciliation · authorized live in the Claude Code session
- 2026-07-22 — transport contract value `streamable-http` → `http` · reversed from 2026-07-16 "transport string `streamable-http` over `http` alias" · `http` is FastMCP 3.x's own default for `run_http_async(transport=)` and the spelling its docstrings/CLI use; both literals route identically (transport.py:337), so this is contract clarity, not behavior
- 2026-07-22 — `_run_kwargs` in `server.py`, not `config.py` nor folded into `from_env` · a translator belongs on the side of the boundary it translates INTO; FastMCP owns `run()`'s signature and `server.py` owns the FastMCP relationship; `config.py` stays framework-free · accepted tradeoff: "host/port only in HTTP mode" now stated twice — local visible duplication over distributed hidden coupling
- 2026-07-23 — `docs/` staging site: lifecycle by location; `docs/spec/` + `docs/session/` transient with delete-on-consume; empty folders = boundary processed · turns a per-piece judgment call into an observable invariant
- 2026-07-23 — two author-scoped scratchpads replace the single shared one · separate-by-file over separate-by-column; one writer per file, both readable
- 2026-07-23 — memory files: Claude Code sole author, both actors propose via own scratchpads · reversed from 2026-07-17 "joint ownership" · joint authorship is a smell; separate proposing from authoring · authorship assigned by principle, not reachability (the connector outage was the test)
- 2026-07-23 — executor rule rewritten in CLAUDE.md *and* working_principles.md: the git boundary is a *proxy* for one-author, not the rule itself
- 2026-07-23 — stage plans migrate in-repo as living `docs/stages/STAGE_0N.md` · dated snapshots hand-roll version control and invite staleness (this one had two stale points); repo authoritative on detail, stage file on sequence + rationale
- 2026-07-23 — scope split: trans-project principles live in memory, project conventions live in-repo · a single repo cannot source a convention that outlives it
- 2026-08-01 — `docs/session/code_session_update.md` exempt from the global show-diff-and-await-approval rule (Art); scope is this file only, not repo edits · sole-authored, gitignored, never committed; the approval round-trip taxed real-time capture and reviewed a file only Claude Code reads; appends happen the moment material surfaces, and Art and the chat see the accumulation at each batch pass
- 2026-09-22 — stages renumbered 1-7 (1 Tools, 2 Local Network, 3 Local DB & Schema, 4 Custom Client, 5 Semantic Search, 6 Recommender, 7 Cloud/MLOps) · reversed from 1, 2, 2.5, 3, 4, 5, 6 · the `.5` was an insertion-order relic that read as a half-step on the stage that gates 5 and 6; cheapest to fix while only STAGE_02.md exists
- 2026-09-22 — one stage, one version tag (v0.2.0 = Stage 2 alone; v0.3.0 = DB; v0.4.0 client; v0.5.0 semantic; v0.6.0 recommender; v1.0.0 cloud) · reversed from "v0.2.0 = Stage 2 + 2.5" · the shared tag was the other half of the `.5` relic
- 2026-09-22 — Pieces 4 (health) and 5 (tests + CI gate) fold into Piece 3 · reason: Piece 3 cannot land correctly without them (custom routes are unauthenticated by construction; a `tests/integration/` suite runs unfiltered in CI today)
- 2026-09-22 — `MCP_HOST` validation stays in Piece 3 as its own commit · reversed from the chat's proposal to move it out · "right idea, right layer, right moment" governs commits, not pieces; a hanging task needs a better reason than "more work"
- 2026-09-22 — Piece 3 extension point is a `TokenVerifier` subclass, not middleware · FastMCP's `Middleware` operates on MCP messages and never sees HTTP headers; `RequireAuthMiddleware` (framework-supplied) issues the 401 and calls the verifier
- 2026-09-22 — new fourth module `auth.py` · `config.py` must stay framework-free and `server.py` MCP-only; a `TokenVerifier` subclass imports `fastmcp`
- 2026-09-22 — `ServerConfig.api_keys: tuple[DeviceKey, ...]`, http-branch scoped, `DeviceKey.token` has `repr=False`; bytes encoding happens in `auth.py` · the device name is required by the `AccessToken.client_id` contract; encoding is an adapter into the `hmac` boundary (Piece 2 translator rule); `hmac.compare_digest` raises `TypeError` on non-ASCII `str` (verified), which bytes make unreachable
- 2026-09-22 — `create_server(config) -> FastMCP` factory, structural commit before the auth commit · reversed from "assign `mcp.auth` in `__main__`" · constructor injection over mutation; tools become plain functions so the 51 existing tests are untouched (verified: tests call `list_recipes()` directly); it is the composition root the DB stage will inject a connection into
- 2026-09-22 — zero API keys in http mode raises `ValueError` at config resolution · fail-closed; four existing config tests gain a key
- 2026-09-22 — empty device suffix, empty token, or duplicate token across devices raises `ValueError`; no minimum length is enforced; `.env.example` documents `python -c "import secrets; print(secrets.token_urlsafe(32))"`
- 2026-09-22 — the `/health` auth bypass is locked by a guard test (same app: `/mcp` without a token -> 401, `/health` -> 200), not a code comment · reversed from STAGE_02's "document the bypass in code comments" · the bypass is a framework property we do not own, so it gets the signature-guard treatment
- 2026-09-22 — pytest markers `integration` (runs in CI, selectable) and `live` (needs a socket or credentials; excluded via `-m "not live"`), registered in `pyproject.toml` in Piece 3 · reversed from Piece 5's `-m "not integration and not network"` · that filter would have excluded the auth tests from CI
- 2026-09-22 — auth HTTP tests use Starlette's `TestClient` (lifespan runs on `__enter__`, verified `testclient.py:687`) with `mcp.http_app(json_response=True)`; no new dev dependency · reversed from the chat's `asgi-lifespan` suggestion
- 2026-09-22 — doc process v4 - `docs/_archive/retired_<pass>/` replaces delete-on-consume; `docs/_inbox/carryover_<pass>.md` for between-session thoughts; `docs/_auxiliary/` for status-tagged temporary proposals · Art's design; the archive also gives the chat a way to audit Code's cross-reference (Code's FLAG)
- 2026-09-22 — pass identity - a pass is one work order and one spec; named by START date `YYYYMMDD` plus `_pN` for the Nth pass of that date (count `_archive/retired_<today>*`, add one); in-flight signal is a non-empty `docs/session/` or `docs/spec/` beyond README keepers · Art's design
- 2026-09-22 — `docs/DOC_PROCESS.md` becomes `docs/SOP.md` carrying pass identity, session-start actions, capture, session-close, archive conventions; `CLAUDE.md` opens with a session-start block that points at it; the seed prompt is one line
- 2026-09-22 — `DECISIONS.md` at repo root is the decision ledger (moved from SUMMARY's Decision Log, append-only, existing line format kept); `docs/adr/` only if an entry ever needs paragraphs · SUMMARY was carrying a ledger and a log in one file
- 2026-09-22 — `docs/HANDOFF.md` retired · every fact in it has a live source the chat can read; it was the file that tempted unique facts into a view · condition: measure the opening read at Pass B's start; revisit if larger than before
- 2026-09-22 — scratchpad types v4 - `DEFERRED:` (vetted, targeted at a stage/piece) routes to `docs/registers/deferred.md`; `FLAG:` narrows to "needs the other actor's call" and routes to `docs/registers/open_questions.md` when unresolved at close; a FLAG answered within the pass is a DECISION
- 2026-09-22 — capture floor ratified - capture if it would change a decision or be lost otherwise (Code's proposal)
- 2026-09-22 — the v3 ordering contradiction resolves by a `## Post-reconciliation` section Code appends to its own scratchpad after the chat's reconciliation timestamp; those entries move to `docs/_inbox/carryover_<pass>.md` at close and open the next pass
- 2026-09-22 — Code's cross-reference discrepancies go to the commit message body (durable, greppable), and the archived spec + scratchpads let the chat audit them
- 2026-09-22 — chat-side backup read path is `secure-shell`, read-only set only (date ls cat head tail grep wc stat diff cmp realpath basename dirname which du shasum md5sum cut tr safe-find); never `safe-git` or any `safe-*` writer from the chat · closes the "backup path" deferred item for reads
- 2026-09-22 — no hard cap on session-start/close step counts; each step names the failure it prevents · reversed from the chat's proposed caps (start <= 5, close <= 7) · caps set before planning are not evidence-based (Art)
- 2026-09-22 — process revamp is Pass A (`20260922`, this chat); Piece 3 is Pass B in a new chat, the first pass run under v4, seeded from `_auxiliary/piece3_design_20260922.md`
- 2026-09-22 — transient naming - `chat_scratchpad_<pass>.md`, `code_scratchpad_<pass>.md`, `spec_<subject>_<pass>.md`, `carryover_<pass>.md`
- 2026-09-23 — registers live at `docs/registers/` (`deferred.md`, `open_questions.md`); `_archive/`, `_inbox/`, `_auxiliary/` live under `docs/` · a file sits at a root only if opened unconditionally at session start; everything else groups by class. Project-root organization is a separate, later discussion
- 2026-09-23 — ledger file name is `DECISIONS.md` (one word, parallel to `CHANGELOG.md`) · reversed from the chat's `DECISION_LOG.md`
- 2026-09-23 — `IDEA:` is a type distinct from `DEFERRED:`; it routes to a new or existing file in `docs/_auxiliary/` (creation is a property of the append) · reversed from the 09-22 collapse into one type · the distinguishing axis is vetting (planned, fits a stage/piece, in scope), which changes the sink class and the write action; a target-or-not split does not
- 2026-09-23 — `MCP_HOST` accepts IP literals only (`ipaddress.ip_address`; hostnames including `localhost` raise) and additionally rejects `0.0.0.0` and `::` · fail-closed: one typo must not turn a LAN server into an all-interfaces server
- 2026-09-23 — a pass resumes in the same chat AND the same Claude Code session until the work order is closed; a chat never spans passes (Art's clause)
